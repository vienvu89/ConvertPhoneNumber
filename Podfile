# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'ConvertNumber' do
    use_frameworks!

    def testing_pods
        pod 'Quick'
        pod 'Nimble'
    end
    target 'ConvertNumberTests' do
        inherit! :search_paths
        # Pods for testing
        testing_pods
    end
end
